# 🚀 Установка PyTorch с CUDA

## ✅ Что было сделано

Запущена установка PyTorch с поддержкой CUDA 11.8:

```powershell
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

## 🔍 Проверка установки

### Вариант 1: Через скрипт проверки

```powershell
python check_cuda.py
```

Этот скрипт покажет:
- ✓ Версию PyTorch
- ✓ Доступность CUDA
- ✓ Информацию о GPU
- ✓ Рекомендации по использованию

### Вариант 2: Через Python команду

```powershell
python -c "import torch; print('CUDA доступна:', torch.cuda.is_available()); print('GPU:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'Нет')"
```

## 📊 Возможные результаты

### ✅ Случай 1: CUDA доступна (есть NVIDIA GPU)

```
CUDA доступна: True
GPU: NVIDIA GeForce RTX 3060
```

**Действия:**
- ✅ Используйте `rag_system_local.py` - будет работать БЫСТРО на GPU
- ⚡ Ожидаемая скорость: 3-5 секунд на ответ (вместо 30-60)

### ❌ Случай 2: CUDA не доступна (нет GPU или драйверов)

```
CUDA доступна: False
GPU: Нет
```

**Действия:**
- ✅ Используйте `rag_system_simple.py` - работает МГНОВЕННО без GPU
- ⚠️ НЕ используйте `rag_system_local.py` - будет очень медленно

## 🎯 Рекомендации

### Если у вас NVIDIA GPU:

1. **Проверьте драйверы:**
```powershell
nvidia-smi
```

Должна показаться информация о GPU и версии драйвера.

2. **Если драйверы есть, но CUDA не работает:**

Возможно нужна другая версия CUDA. Попробуйте:

```powershell
# Для CUDA 12.1
pip uninstall torch torchvision torchaudio
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Для CUDA 11.8 (уже установлено)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

3. **Запустите локальную версию:**
```powershell
python rag_system_local.py
```

### Если у вас НЕТ NVIDIA GPU (Intel/AMD):

1. **Используйте быструю версию:**
```powershell
python rag_system_simple.py
```

Эта версия:
- ⚡ Работает МГНОВЕННО (< 1 сек)
- ✅ Не требует GPU
- ✅ Дает точные структурированные ответы

2. **Или используйте OpenAI версию (с VPN):**
```powershell
python rag_system.py
```

## 📋 Сравнение скорости

| Конфигурация | Версия | Скорость ответа |
|--------------|--------|-----------------|
| **NVIDIA GPU + CUDA** | rag_system_local.py | ⚡ 3-5 сек |
| **CPU + OpenAI API** | rag_system.py | ⚡ 1-2 сек |
| **Любая (без LLM)** | rag_system_simple.py | 🚀 < 1 сек |
| **CPU без GPU** | rag_system_local.py | 🐌 30-60 сек |

## 🔧 Устранение проблем

### Проблема: "CUDA не доступна" при наличии NVIDIA GPU

**Решение 1:** Обновите драйверы NVIDIA
- Скачайте с https://www.nvidia.com/Download/index.aspx
- Установите последнюю версию

**Решение 2:** Установите CUDA Toolkit
- Скачайте с https://developer.nvidia.com/cuda-downloads
- Установите версию 11.8 или 12.1

**Решение 3:** Переустановите PyTorch
```powershell
pip uninstall torch torchvision torchaudio
pip cache purge
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### Проблема: Установка PyTorch занимает много времени

Это нормально! PyTorch с CUDA весит ~2-3 GB. Подождите завершения установки.

### Проблема: Ошибка при установке

Попробуйте установить CPU версию:
```powershell
pip install torch torchvision torchaudio
```

Затем используйте `rag_system_simple.py` (самая быстрая версия).

## ✅ Итоговая проверка

После установки выполните:

```powershell
# 1. Проверка CUDA
python check_cuda.py

# 2. Если CUDA доступна - запуск локальной версии
python rag_system_local.py

# 3. Если CUDA не доступна - запуск быстрой версии
python rag_system_simple.py
```

## 💡 Финальная рекомендация

**Независимо от наличия GPU, попробуйте сначала:**

```powershell
python rag_system_simple.py
```

Эта версия:
- 🚀 Работает мгновенно на любом компьютере
- ✅ Не требует GPU, API ключей, VPN
- ✅ Дает структурированные точные ответы
- ✅ Идеально для разработки и демо

---

**Проверьте установку прямо сейчас:**

```powershell
python check_cuda.py
```


