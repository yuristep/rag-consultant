# RAG-система для консультаций по ювелирным изделиям

Интеллектуальный ассистент для консультирования клиентов по ювелирным изделиям с использованием технологии RAG (Retrieval-Augmented Generation).

## 🎯 Возможности

- Ответы на вопросы о ювелирных изделиях
- Рекомендации по выбору украшений
- Консультации по уходу за изделиями
- Поиск по каталогу товаров
- Использование базы знаний из документов

## 🛠 Технологии

- **LangChain** - фреймворк для работы с LLM
- **FAISS** - векторное хранилище для быстрого поиска
- **OpenAI GPT-4o-mini** - языковая модель для генерации ответов
- **HuggingFace Embeddings** - мультиязычная модель эмбеддингов

## 📋 Требования

- Python 3.8+
- Виртуальное окружение (venv или .venv)
- Установленные зависимости из `requirements.txt`

## 🚀 Установка

1. Клонируйте репозиторий или скопируйте файлы

2. Создайте виртуальное окружение (если еще не создано):
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
```powershell
# Windows PowerShell
.\venv\Scripts\Activate.ps1

# Windows CMD
venv\Scripts\activate.bat
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

## 🔑 Настройка API ключей

Откройте файл `rag_system.py` и укажите ваши API ключи:

```python
os.environ["OPENAI_API_KEY"] = "ваш-ключ-openai"
os.environ["UNSTRUCTURED_API_KEY"] = "ваш-ключ-unstructured"  # опционально
```

**Рекомендация:** Для безопасности используйте файл `.env` и библиотеку `python-dotenv`.

## 📂 Структура проекта

```
PE6.5/
├── rag_system.py          # Основной модуль RAG-системы
├── run_rag.py             # Скрипт запуска с настройкой кодировки
├── run.ps1                # PowerShell скрипт для запуска
├── requirements.txt       # Зависимости Python
├── catalog.json          # Каталог товаров (создается автоматически)
├── data/                 # Директория для исходных документов (PDF)
├── output/               # Обработанные документы (JSON)
└── faiss_index/          # Векторная база данных FAISS
```

## ▶️ Запуск

### 🚀 Быстрая версия (РЕКОМЕНДУЕТСЯ)

```powershell
python rag_system_simple.py
```

**Преимущества:** Мгновенная работа, не требует API ключей и VPN

### ⚡ Версия с OpenAI (нужен VPN)

```powershell
python rag_system.py
```

**Преимущества:** Лучшее качество ответов, естественный язык

### 🔧 Локальная версия с Llama (медленно)

```powershell
python rag_system_local.py
```

**Преимущества:** Полностью автономная, не требует API

📊 **Подробное сравнение версий:** см. файл `СРАВНЕНИЕ_ВЕРСИЙ.md`

## 💬 Использование

После запуска система инициализируется и предложит ввести вопрос:

```
============================================================
RAG-СИСТЕМА: КОНСУЛЬТАНТ ПО ЮВЕЛИРНЫМ ИЗДЕЛИЯМ
============================================================

Добро пожаловать! Я помогу вам с выбором ювелирных изделий.
Введите 'exit', 'выход' или 'quit' для завершения

Вы: Какие серьги подойдут для вечернего мероприятия?

Ассистент: Для вечернего мероприятия отлично подойдут...
```

### Примеры вопросов:

- "Какие украшения подойдут для свадьбы?"
- "Как ухаживать за серебряными изделиями?"
- "Посоветуй подарок на юбилей"
- "Что такое проба золота?"
- "Покажи украшения с бриллиантами"

## 📚 Добавление документов

1. Поместите PDF-файлы с информацией в директорию `data/`
2. Запустите систему - документы будут автоматически обработаны
3. Обработанные данные сохранятся в `output/`
4. Векторная база обновится автоматически

## 🔧 Настройка

### Изменение модели

В файле `rag_system.py` можно изменить:

```python
# Модель эмбеддингов
self.embeddings_model = HuggingFaceEmbeddings(
    model_name="intfloat/multilingual-e5-large",  # замените на другую
    model_kwargs={'device': 'cpu'}
)

# Языковая модель
self.llm = ChatOpenAI(
    model_name="gpt-4o-mini",  # или gpt-4, gpt-3.5-turbo
    temperature=0.5
)
```

### Настройка поиска

```python
# Количество релевантных документов
self.retriever = self.db.as_retriever(
    search_type="similarity",
    search_kwargs={"k": 4}  # измените количество
)
```

## 🐛 Решение проблем

### Проблема с кодировкой

Если русские символы отображаются некорректно:

```powershell
chcp 65001
[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new($false)
$env:PYTHONIOENCODING='utf-8'
```

### Ошибка импорта модулей

Убедитесь, что виртуальное окружение активировано и зависимости установлены:

```bash
pip install -r requirements.txt
```

### Ошибка API ключа

Проверьте, что API ключ OpenAI указан правильно и имеет доступ к модели GPT-4o-mini.

## 📝 Лицензия

Проект создан в образовательных целях.

## 👥 Автор

Разработано для курса по машинному обучению и NLP.

## 🔄 Обновления

### Версия 2.0 (текущая)
- ✅ Переработан для локального запуска
- ✅ Удалены комментарии и код из Colab
- ✅ Добавлена объектно-ориентированная структура
- ✅ Улучшена обработка ошибок
- ✅ Добавлена настройка кодировки для Windows
- ✅ Создана документация

### Версия 1.0
- Базовая версия из Google Colab

