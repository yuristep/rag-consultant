# 🚀 Инструкция по запуску RAG-системы

## ✅ Что было сделано

Ваша RAG-система полностью переработана для локального запуска:

- ✅ Удалены все комментарии и код из Colab
- ✅ Создана объектно-ориентированная архитектура (класс `RAGSystem`)
- ✅ Добавлена правильная обработка ошибок
- ✅ Настроена поддержка UTF-8 для Windows
- ✅ Создана полная документация

## 📁 Новые файлы

- **`rag_system.py`** - основной модуль системы (чистая версия)
- **`run_rag.py`** - скрипт запуска с настройкой кодировки
- **`run.ps1`** - PowerShell скрипт для запуска
- **`ЗАПУСК.bat`** - Batch файл для запуска
- **`README.md`** - полная документация
- **`test_rag.py`** - тестовый скрипт для проверки

## 🎯 Запуск в терминале Cursor

### Шаг 1: Откройте терминал в Cursor

Нажмите `` Ctrl+` `` или выберите `Terminal → New Terminal`

### Шаг 2: Активируйте виртуальное окружение (если не активировано)

```powershell
.\.venv\Scripts\Activate.ps1
```

Вы должны увидеть `(.venv)` в начале строки.

### Шаг 3: Настройте кодировку UTF-8

```powershell
chcp 65001
[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new($false)
$env:PYTHONIOENCODING='utf-8'
$env:PYTHONUTF8='1'
```

### Шаг 4: Запустите систему

**Вариант A - Через скрипт запуска:**
```powershell
python run_rag.py
```

**Вариант B - Напрямую:**
```powershell
python rag_system.py
```

**Вариант C - Тестовый запуск:**
```powershell
python test_rag.py
```

### Шаг 5: Работа с системой

После инициализации (займёт 30-60 секунд) вы увидите:

```
============================================================
RAG-СИСТЕМА: КОНСУЛЬТАНТ ПО ЮВЕЛИРНЫМ ИЗДЕЛИЯМ
============================================================

Добро пожаловать! Я помогу вам с выбором ювелирных изделий.
Введите 'exit', 'выход' или 'quit' для завершения

Вы: 
```

Введите ваш вопрос, например:
- "Какие серьги подойдут для вечернего мероприятия?"
- "Посоветуй украшение для подарка"
- "Как ухаживать за серебром?"

Для выхода введите: `exit`, `выход` или `quit`

## 🔧 Если что-то не работает

### Проблема 1: Ошибка импорта модулей

```powershell
pip install -r requirements.txt
```

### Проблема 2: Ошибка API ключа OpenAI

Откройте файл `rag_system.py` и замените ключ на строке 29:

```python
os.environ["OPENAI_API_KEY"] = "ваш-ключ-здесь"
```

### Проблема 3: Русские символы отображаются некорректно

Повторите Шаг 3 (настройка кодировки)

### Проблема 4: Программа не запускается

Проверьте, что вы находитесь в правильной директории:

```powershell
cd C:\Users\y.stepanov\PycharmProjects\PE6.5
```

## 📊 Тестирование системы

Запустите тестовый скрипт:

```powershell
python test_rag.py
```

Он проверит:
1. ✓ Импорт всех модулей
2. ✓ Создание экземпляра RAGSystem
3. ✓ Инициализацию эмбеддингов
4. ✓ Загрузку базы знаний
5. ✓ Создание базы каталога
6. ✓ Инициализацию LLM
7. ✓ Создание RAG цепочки

## 🎨 Альтернативные способы запуска

### Через PowerShell скрипт:

```powershell
.\run.ps1
```

### Через Batch файл:

Двойной клик на файле `ЗАПУСК.bat` в проводнике Windows

### Через Python напрямую:

```powershell
python -c "from rag_system import main; main()"
```

## 📝 Примечания

1. **Первый запуск** займёт больше времени (загрузка моделей)
2. **API ключ OpenAI** должен быть действительным
3. **Интернет соединение** требуется для работы с OpenAI API
4. **Виртуальное окружение** должно быть активировано

## 💡 Полезные команды

Проверить версию Python:
```powershell
python --version
```

Проверить установленные пакеты:
```powershell
pip list
```

Обновить пакеты:
```powershell
pip install --upgrade -r requirements.txt
```

## 📚 Дополнительная информация

Полная документация находится в файле `README.md`

---

**Готово!** Ваша RAG-система полностью настроена и готова к использованию! 🎉

