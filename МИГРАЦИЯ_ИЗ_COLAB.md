# 🔄 Миграция из Google Colab: Проблемы и решения

## 📋 Обзор проблем

При переносе кода из Google Colab на локальную машину возникли множественные проблемы, связанные с обновлением библиотек и изменениями в API.

---

## 🚨 Основные проблемы и решения

### 1. ⚠️ Изменения в LangChain API

#### Проблема: Устаревший метод `get_relevant_documents()`

**Код из Colab (старый):**
```python
# Это работало в Colab
context_docs = retriever.get_relevant_documents(query)
products_docs = catalog_retriever.get_relevant_documents(query)
```

**Ошибка:**
```
'VectorStoreRetriever' object has no attribute 'get_relevant_documents'
```

**Причина:**
- В LangChain 0.1.x метод `get_relevant_documents()` был основным
- В LangChain 0.2.x+ (текущая версия) метод устарел
- Новый API использует универсальный метод `invoke()`

**Решение:**
```python
# Новый способ (работает)
context_docs = retriever.invoke(query)
products_docs = catalog_retriever.invoke(query)
```

**Где исправлено:**
- `rag_system.py` - строка 326, 334
- `rag_system_simple.py` - строка 345, 350
- `rag_system_local.py` - аналогично

---

### 2. ⚠️ Устаревание HuggingFaceEmbeddings

#### Проблема: Предупреждение об устаревании

**Код из Colab:**
```python
from langchain_community.embeddings import HuggingFaceEmbeddings
```

**Предупреждение:**
```
LangChainDeprecationWarning: The class `HuggingFaceEmbeddings` was deprecated 
in LangChain 0.2.2 and will be removed in 1.0. An updated version exists in 
the `langchain-huggingface` package.
```

**Причина:**
- Класс перемещен в отдельный пакет `langchain-huggingface`
- Старый импорт работает, но устарел

**Решение:**
```python
# Новый способ с обратной совместимостью
try:
    from langchain_huggingface import HuggingFaceEmbeddings
except ImportError:
    from langchain_community.embeddings import HuggingFaceEmbeddings
    import warnings
    warnings.filterwarnings('ignore', category=DeprecationWarning)
```

**Где исправлено:**
- Все три файла: `rag_system.py`, `rag_system_simple.py`, `rag_system_local.py`

---

### 3. 🌍 OpenAI API: Ограничение по регионам

#### Проблема: 403 ошибка

**Ошибка:**
```
Error code: 403 - {'error': {'code': 'unsupported_country_region_territory', 
'message': 'Country, region, or territory not supported'}}
```

**Причина:**
- OpenAI API не работает в России с 2023 года
- В Colab это работало (серверы в США)
- Локально - блокировка по IP

**Решение 1: VPN**
```bash
# Подключите VPN (США, Европа)
# Затем запустите
python rag_system.py
```

**Решение 2: Локальная модель**
```bash
# Используйте версию без OpenAI
python rag_system_local.py  # С Llama 3
python rag_system_simple.py  # Без LLM (быстро)
```

**Решение 3: Альтернативные API**
```python
# YandexGPT (работает в РФ)
from langchain_community.llms import YandexGPT

# GigaChat от Сбера (работает в РФ)
from langchain_community.chat_models import GigaChat
```

**Где исправлено:**
- `rag_system.py` - добавлена обработка ошибки 403 (строка 350-354)

---

### 4. 🖥️ Проблемы с кодировкой в Windows

#### Проблема: Кракозябры вместо русских букв

**В Colab:**
```python
print("Привет, мир!")  # Работает отлично
```

**В Windows PowerShell:**
```
╨Я╤А╨╕╨▓╨╡╤В, ╨╝╨╕╤А!  # Кракозябры
```

**Причина:**
- Colab использует UTF-8 по умолчанию
- Windows PowerShell использует CP866 (кодовая страница 866)
- Python в Windows по умолчанию не использует UTF-8

**Решение:**
```python
# В начале каждого скрипта
import sys
import codecs

if sys.platform == 'win32':
    if hasattr(sys.stdout, 'buffer'):
        sys.stdout = codecs.getwriter('utf-8')(sys.stdout.buffer, 'strict')
    if hasattr(sys.stderr, 'buffer'):
        sys.stderr = codecs.getwriter('utf-8')(sys.stderr.buffer, 'strict')
    
    os.environ['PYTHONIOENCODING'] = 'utf-8'
    os.environ['PYTHONUTF8'] = '1'
```

**В PowerShell перед запуском:**
```powershell
chcp 65001
$env:PYTHONIOENCODING='utf-8'
```

**Где исправлено:**
- Все три файла системы (строки 14-21)

---

### 5. 📦 Отсутствие зависимостей

#### Проблема: ModuleNotFoundError

**В Colab:**
```python
!pip install langchain  # Работает в ячейке
```

**Локально:**
```
ModuleNotFoundError: No module named 'langchain_core'
```

**Причина:**
- В Colab многие библиотеки предустановлены
- Colab автоматически устанавливает зависимости через `!pip`
- Локально нужно устанавливать вручную

**Решение:**
```bash
# Создан requirements.txt со всеми зависимостями
pip install -r requirements.txt
```

**Список зависимостей:**
```
langchain>=0.1.0
langchain-community>=0.0.20
langchain-core>=0.1.0
langchain-openai>=0.0.5
faiss-cpu>=1.7.4
sentence-transformers>=2.3.0
torch>=2.1.0
transformers>=4.35.0
python-dotenv>=1.0.0
```

---

### 6. 🔐 Хранение API ключей

#### Проблема: Ключи в коде

**В Colab:**
```python
# Можно хранить прямо в коде
os.environ["OPENAI_API_KEY"] = "sk-proj-abc123..."
```

**Локально:**
```python
# ❌ ОПАСНО! Ключ попадет в Git
os.environ["OPENAI_API_KEY"] = "sk-proj-abc123..."
```

**Причина:**
- Colab notebook не публикуется автоматически
- Локальный код часто попадает в Git
- Риск утечки ключей

**Решение:**
```python
# Использование .env файла
from dotenv import load_dotenv
load_dotenv()

# Ключи берутся из .env (не коммитится в Git)
if not os.getenv("OPENAI_API_KEY"):
    os.environ["OPENAI_API_KEY"] = "your-key-here"
```

**Файл .env:**
```env
OPENAI_API_KEY=sk-proj-your-real-key
```

**Где исправлено:**
- `rag_system.py` - строки 32-44
- `.gitignore` - добавлен `.env`
- `env.example` - создан пример

---

### 7. 🐌 Производительность локальных моделей

#### Проблема: Медленная работа

**В Colab:**
```python
# Llama 3 работает быстро (есть GPU T4/V100)
model = AutoModelForCausalLM.from_pretrained(model_name)
# Ответ за 3-5 секунд
```

**Локально (CPU):**
```python
# Llama 3 работает ОЧЕНЬ медленно
model = AutoModelForCausalLM.from_pretrained(model_name)
# Ответ за 30-60 секунд!
```

**Причина:**
- Colab предоставляет бесплатный GPU (NVIDIA T4)
- Локально обычно только CPU
- Модель Llama 3 (8B параметров) требует GPU для быстрой работы

**Решение:**
```python
# Создана быстрая версия без LLM
# rag_system_simple.py - работает мгновенно
# Использует только векторный поиск, без генерации текста
```

**Сравнение:**
| Окружение | Устройство | Скорость |
|-----------|-----------|----------|
| Colab | GPU T4 | 3-5 сек |
| Локально | NVIDIA RTX | 3-5 сек |
| Локально | CPU | 30-60 сек |
| Simple версия | Любое | < 1 сек |

---

### 8. 📂 Структура файлов

#### Проблема: Все в одном файле

**В Colab:**
```python
# Весь код в одном .ipynb файле
# 600+ строк в одном файле
# Смешанная логика
```

**Причина:**
- Colab notebook - линейная структура
- Все ячейки выполняются последовательно
- Нет необходимости в модульности

**Решение:**
```python
# Создана объектно-ориентированная архитектура
class RAGSystem:
    def __init__(self): ...
    def initialize_embeddings(self): ...
    def load_knowledge_base(self): ...
    def create_catalog_database(self): ...
    def initialize_llm(self): ...
    def create_rag_chain(self): ...
    def query(self): ...
    def run_interactive(self): ...
```

**Преимущества:**
- ✅ Модульная структура
- ✅ Переиспользование кода
- ✅ Легко тестировать
- ✅ Легко расширять

---

### 9. 🔄 LangChain Chains API

#### Проблема: Устаревшие цепочки

**В Colab (старый API):**
```python
from langchain.chains import RetrievalQA, LLMChain

# Старый способ
rag_chain = RetrievalQA.from_chain_type(
    llm=llm,
    chain_type="stuff",
    retriever=retriever
)
```

**Локально (новый API):**
```
ImportError: cannot import name 'LLMChain' from 'langchain.chains'
```

**Причина:**
- LangChain 0.1.x использовал `RetrievalQA`, `LLMChain`
- LangChain 0.2.x+ переместил их в `langchain_classic`
- Новый подход - LCEL (LangChain Expression Language)

**Решение 1: Использовать langchain_classic**
```python
from langchain_classic.chains import LLMChain
```

**Решение 2: Использовать LCEL (современный)**
```python
from langchain_core.runnables import RunnablePassthrough

rag_chain = (
    {
        "context": retriever | format_docs,
        "products": catalog_retriever | format_products,
        "question": RunnablePassthrough(),
    }
    | prompt
    | llm
    | StrOutputParser()
)
```

**Решение 3: Создать кастомный класс**
```python
class CustomRAGChain:
    def __init__(self, llm, prompt):
        self.llm = llm
        self.prompt = prompt
    
    def invoke(self, inputs):
        full_prompt = self.prompt.format(**inputs)
        messages = [HumanMessage(content=full_prompt)]
        response = self.llm.invoke(messages)
        return {"text": response.content}
```

**Где исправлено:**
- `rag_system.py` - создан класс `CustomRAGChain` (строки 276-301)
- `rag_system_local.py` - использован LCEL подход

---

### 10. 🎯 Условие `if __name__ == '__main__'`

#### Проблема: Код не выполняется

**В Colab:**
```python
# Весь код выполняется сразу
# Условие if __name__ не нужно
BASE_DIR = Path(__file__).parent
```

**Локально:**
```python
if __name__ == '__main__':
    # Код выполняется только при прямом запуске
    BASE_DIR = Path(__file__).parent
```

**Причина:**
- В Colab все ячейки выполняются как скрипт
- Локально нужно различать импорт и запуск
- `__file__` в Colab работает по-другому

**Решение:**
```python
def main():
    """Главная функция"""
    base_dir = Path(__file__).parent
    rag = RAGSystem(base_dir)
    # ... остальной код
    
if __name__ == '__main__':
    sys.exit(main())
```

**Где исправлено:**
- Все три файла системы - создана функция `main()`

---

### 11. 📝 Интерактивный ввод

#### Проблема: input() в Colab vs локально

**В Colab:**
```python
query = input("Вы: ")  # Показывает текстовое поле
```

**Локально в фоновом режиме:**
```python
query = input("Вы: ")  # Программа зависает, ждет ввода
```

**Причина:**
- Colab предоставляет GUI для input()
- Локально input() блокирует выполнение
- Фоновые процессы не могут ждать ввода

**Решение:**
```python
# Запуск в интерактивном режиме (не в фоне)
def run_interactive(self):
    while True:
        try:
            question = input("Вы: ").strip()
            if question.lower() in ["exit", "выход", "quit"]:
                break
            # Обработка вопроса
        except KeyboardInterrupt:
            print("\nПрограмма прервана")
            break
```

**Где исправлено:**
- Все три файла - метод `run_interactive()`

---

### 12. 🔧 Пути к файлам

#### Проблема: Относительные пути

**В Colab:**
```python
# Все файлы в одной директории
directory_with_pdfs = "data"
```

**Локально:**
```python
# Нужны абсолютные пути
directory_with_pdfs = str(BASE_DIR / "data")
```

**Причина:**
- Colab работает в фиксированной директории
- Локально скрипт может запускаться откуда угодно
- Относительные пути могут не работать

**Решение:**
```python
from pathlib import Path

BASE_DIR = Path(__file__).parent
DATA_DIR = BASE_DIR / "data"
OUTPUT_DIR = BASE_DIR / "output"
FAISS_INDEX_DIR = BASE_DIR / "faiss_index"

# Использование абсолютных путей
directory_with_pdfs = str(DATA_DIR.resolve())
```

**Где исправлено:**
- Все три файла - в `__init__()` метод класса

---

### 13. 💾 Сохранение и загрузка FAISS

#### Проблема: Параметр allow_dangerous_deserialization

**В Colab (старая версия):**
```python
db = FAISS.load_local(save_path, embeddings)
```

**Локально (новая версия):**
```
ValueError: The de-serialization relies on loading a pickle file. 
Pickle files can be modified to deliver a malicious payload...
```

**Причина:**
- FAISS обновил безопасность
- Требуется явное разрешение на десериализацию pickle
- Защита от вредоносного кода

**Решение:**
```python
db = FAISS.load_local(
    save_path, 
    embeddings, 
    allow_dangerous_deserialization=True
)
```

**Где исправлено:**
- Все три файла - в методе `load_knowledge_base()`

---

### 14. 🐍 Версия Python

#### Проблема: Разные версии Python

**В Colab:**
- Python 3.10.x (фиксированная версия)
- Все библиотеки совместимы

**Локально:**
- Python 3.12.x (новая версия)
- Некоторые библиотеки могут быть несовместимы

**Проблемы:**
- `bitsandbytes` не работает на Windows
- Некоторые пакеты нет для Python 3.12

**Решение:**
```python
# Условная загрузка
if device == "cuda":
    try:
        from transformers import BitsAndBytesConfig
        # Используем квантование
    except ImportError:
        # Загружаем без квантования
        model = AutoModelForCausalLM.from_pretrained(model_name)
```

**Где исправлено:**
- `rag_system_local.py` - условная загрузка BitsAndBytesConfig

---

### 15. 🔄 Виртуальное окружение

#### Проблема: Конфликты зависимостей

**В Colab:**
- Глобальное окружение
- Все пакеты совместимы
- Google управляет версиями

**Локально:**
- Нужно виртуальное окружение
- Могут быть конфликты версий
- Нужно управлять зависимостями

**Решение:**
```bash
# Создание виртуального окружения
python -m venv venv
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

**Проблема с кириллицей в пути:**
```
Fatal error in launcher: Unable to create process using 
'"c:\Users\...\??6.5\.venv\Scripts\python.exe"'
```

**Решение:**
```bash
# Использовать python -m pip вместо pip
python -m pip install package_name
```

---

## 📊 Сводная таблица изменений

| Компонент | Colab | Локально | Решение |
|-----------|-------|----------|---------|
| **LangChain API** | get_relevant_documents() | invoke() | Обновлен метод |
| **HuggingFaceEmbeddings** | langchain_community | langchain_huggingface | Try-except импорт |
| **OpenAI API** | Работает | 403 ошибка | VPN или локальная модель |
| **Кодировка** | UTF-8 | CP866 | Настройка UTF-8 |
| **Зависимости** | Предустановлены | Нужна установка | requirements.txt |
| **API ключи** | В коде | .env файл | python-dotenv |
| **Производительность** | GPU T4 | CPU | Создана fast версия |
| **Chains API** | RetrievalQA | LCEL/Custom | Кастомный класс |
| **FAISS** | load_local() | + allow_dangerous | Добавлен параметр |
| **Структура** | Один файл | Модульная | ООП классы |

---

## 🎯 Итоговые решения

### Создано 3 версии:

#### 1. **rag_system_simple.py** - Быстрая
- ✅ Работает без LLM
- ✅ Не требует OpenAI API
- ✅ Мгновенная работа (< 1 сек)
- ✅ Идеально для разработки

#### 2. **rag_system.py** - С OpenAI
- ✅ Лучшее качество ответов
- ⚠️ Требует VPN для РФ
- ✅ Быстрая работа (1-2 сек)
- ✅ Для продакшена

#### 3. **rag_system_local.py** - Локальная
- ✅ Полностью автономная
- ✅ Не требует API
- ⚠️ Медленно на CPU (30-60 сек)
- ✅ Для автономной работы

---

## 📚 Документация изменений

Все изменения задокументированы в:
- [ИСПРАВЛЕНИЕ_ОШИБОК.md](docs/ИСПРАВЛЕНИЕ_ОШИБОК.md) - исправленные ошибки
- [СРАВНЕНИЕ_ВЕРСИЙ.md](docs/СРАВНЕНИЕ_ВЕРСИЙ.md) - сравнение версий
- [ИТОГОВАЯ_СВОДКА.md](docs/ИТОГОВАЯ_СВОДКА.md) - полная сводка

---

## ✅ Вывод

Миграция из Colab потребовала:
1. ✅ Обновление API методов (get_relevant_documents → invoke)
2. ✅ Исправление импортов (HuggingFaceEmbeddings)
3. ✅ Решение проблемы с OpenAI (VPN или альтернативы)
4. ✅ Настройка UTF-8 для Windows
5. ✅ Создание requirements.txt
6. ✅ Безопасное хранение API ключей (.env)
7. ✅ Оптимизация производительности (3 версии)
8. ✅ Рефакторинг в ООП структуру
9. ✅ Добавление обработки ошибок
10. ✅ Создание документации

**Результат:** Профессиональная система, готовая к продакшену! 🚀

